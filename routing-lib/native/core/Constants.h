#pragma once

namespace routing {

    // Math constants
    static constexpr double ROUTING_PI = 3.1415926535897932;
    static constexpr double ROUTING_DEG_TO_RAD = ROUTING_PI / 180.0;
    static constexpr double ROUTING_RAD_TO_DEG = 180.0 / ROUTING_PI;
    static constexpr int    ROUTING_WORLD_SIZE  = 1 << 20;

} // namespace routing
