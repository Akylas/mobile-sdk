// swift-tools-version:5.4.0

import PackageDescription

let package = Package(
    name: "$frameworkName",
    platforms: [
        .iOS(.v12)
    ],
    products: [
        .library(
            name: "$frameworkName",
            targets: ["$frameworkName"]
        )
    ],
    dependencies: [
    ],
    targets: [
        .binaryTarget(
            name: "$frameworkName",
            url: "https://github.com/Akylas/mobile-sdk/releases/download/$version/$distName",
            checksum: "$checksum"
        )
    ]
)